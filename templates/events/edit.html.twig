{% extends 'base.html.twig' %}

{% block title %}Edit Events{% endblock %}

{% block body %}
<div class="body">
    <div class="container pb-5">

        <a href="{{ path('app_events_index') }}" class="btn btnBack my-5">Back</a>

        <h2 class="pb-5 headers">Edit event</h2>

        {{ include('events/_form.html.twig', {'button_label': 'Edit'}) }}

        {# {{ include('events/_delete_form.html.twig') }} #}
    </div>
</div>
{% endblock %}
